run jupyter notebook to launch notebook server

create .ipynb files to write and execute Python code interactively

In the Jupyter interface, click on New → Python 3 (ipykernel) to create a new notebook.